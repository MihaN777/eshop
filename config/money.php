<?php

return [

    /*
    |--------------------------------------------------------------------------
    | Валюта магазина по умолчанию
    |--------------------------------------------------------------------------
    |
    | Этой валютой оперируют все Price, у которых валюта
    | не указана явно (чтение из БД, суммы корзины и заказа).
    |
    */

    'default_currency' => env('MONEY_DEFAULT_CURRENCY', 'RUB'),

    /*
    |--------------------------------------------------------------------------
    | Режим сравнения и отображения цен
    |--------------------------------------------------------------------------
    |
    | 'strict'     — точность до копеек (2 знака): сравнение по минорным единицам,
    |                вывод с копейками ("1 000,50 ₽").
    | 'non_strict' — копейки не учитываются: сравнение по целым рублям (усечение,
    |                не округление), вывод без копеек ("1 000 ₽").
    |
    | По умолчанию 'strict' — платёжная сверка не должна молча принимать недоплату
    | в копейках (если такие имеются). Переключите на 'non_strict', если магазин работает лишь в целых валютах.
    |
    */

    'mode' => env('MONEY_MODE', 'strict'),

    /*
    |--------------------------------------------------------------------------
    | Метаданные валют
    |--------------------------------------------------------------------------
    |
    | symbol — символ для отображения; scale — число знаков минорной единицы
    | (для рубля/доллара это 2: копейки/центы).
    |
    */

    'currencies' => [
        'RUB' => ['symbol' => '₽', 'scale' => 2],
        'USD' => ['symbol' => '$', 'scale' => 2],
    ],

    /*
    |--------------------------------------------------------------------------
    | Формат вывода
    |--------------------------------------------------------------------------
    |
    | Разделители для number_format() при приведении Price к строке.
    |
    */

    'format' => [
        'decimals' => ',',
        'thousands' => ' ',
    ],

];
