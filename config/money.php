<?php

return [

    /*
    |--------------------------------------------------------------------------
    | Валюта магазина по умолчанию
    |--------------------------------------------------------------------------
    |
    | Этой валютой оперируют все Price, у которых валюта
    | не указана явно (чтение из БД, суммы корзины и заказа).
    |
    */

    'default_currency' => env('MONEY_DEFAULT_CURRENCY', 'RUB'),

    /*
    |--------------------------------------------------------------------------
    | Метаданные валют
    |--------------------------------------------------------------------------
    |
    | symbol — символ для отображения; scale — число знаков минорной единицы
    | (для рубля/доллара это 2: копейки/центы).
    |
    */

    'currencies' => [
        'RUB' => ['symbol' => '₽', 'scale' => 2],
        'USD' => ['symbol' => '$', 'scale' => 2],
    ],

    /*
    |--------------------------------------------------------------------------
    | Формат вывода
    |--------------------------------------------------------------------------
    |
    | Разделители для number_format() при приведении Price к строке.
    |
    */

    'format' => [
        'RUB' => ['decimals' => ',', 'thousands' => ' '],
        'USD' => ['decimals' => '.', 'thousands' => ' '],
    ],

];
