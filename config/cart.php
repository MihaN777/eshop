<?php

return [

    /*
    |--------------------------------------------------------------------------
    | Потолок количества одного товара в заказе
    |--------------------------------------------------------------------------
    |
    | Ограничивает суммарное количество одного товара в корзине (по всем строкам,
    | т.к. разные наборы опций дают разные строки). Без такого потолка один заказ
    | может выкупить весь остаток позиции и удерживать его до автоотмены.
    |
    | Переопределяется для конкретного товара колонкой products.max_order_quantity.
    |
    */

    'max_quantity_per_product' => (int) env('CART_MAX_QUANTITY_PER_PRODUCT', 20),

];
